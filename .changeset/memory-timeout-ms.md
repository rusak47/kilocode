---
"kilo-code": minor
---

`memory.timeout_ms` overrides the timeout for memory consolidation model calls (default 30000ms, floored at 1000); set it when reasoning-heavy models run long instead of capping output.
